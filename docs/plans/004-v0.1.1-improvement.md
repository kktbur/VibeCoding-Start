# Plan 004: VibeCoding Start v0.1.1 Improvement

- Status: ACTIVE — M1 implementation in progress
- Date: 2026-09-05
- Input: maintainer-provided VibeCoding-Start improvement plan
- Previous release: [v0.1.0](https://github.com/kktbur/VibeCoding-Start/releases/tag/v0.1.0)

## Scope

This plan executes the v0.1.1 maintenance milestone without changing the Skill-only Plugin boundary or rewriting the frozen v0.1.0 tag. The completed v0.1.0 tag and Release close the original M0 dependency; M1 starts from the current `main` baseline.

## M1 work packages

- WP-11: rewrite the public README around final installation instructions, badges, documentation links, privacy, and concise troubleshooting.
- WP-12: add a Keep a Changelog file, move the Plugin to version `0.1.1`, and enforce Plugin/CHANGELOG consistency in CI.
- WP-13: reduce `audit_docs.py` false positives for headings, lifecycle words, ignore rules, and invalid UTF-8 while preserving true negative cases.
- WP-14: establish LF text conventions with `.gitattributes`, `.editorconfig`, and a deterministic line-ending check.
- WP-15: rename the repository marketplace identifier from `personal` to `kktbur`; document the migration in the changelog.
- WP-16: refresh active project knowledge and document equivalent PowerShell 7 and Unix-like checks.

## Explicit non-goals

Do not add databases, vector search, new test frameworks, MCP/App infrastructure, Docker smoke testing, clean-room installation matrices, release automation, or the v0.2.0 example/small-project behavior in this milestone.

## Acceptance

- Existing true-negative fixtures continue to fail as intended.
- New subtitle, Chinese heading, Chinese lifecycle, directory-ignore, and invalid-UTF-8 cases prove the reduced false-positive surface.
- Plugin, fixture, audit, link, freshness, name, version, line-ending, and compile checks pass.
- README contains no pre-merge release-process instructions and its marketplace name matches the catalog.
- The owner can inspect the diff, run the documented checks, and roll back the M1 commit or PR without rewriting history.

## Release and rollback

The v0.1.1 tag and GitHub Release are a separate G8 checkpoint after review and CI. If the marketplace rename or audit changes cause a regression, revert the corresponding M1 change and retain the v0.1.0 tag as the last released package.

